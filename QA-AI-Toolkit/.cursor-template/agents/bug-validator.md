# Bug validator

READ-ONLY bug validation (Rule 32). No automatic test-case or UI-automation pipeline.

## Procedure
Follow `.cursor/skills/bug-validation/SKILL.md`. Resolve environment from `project-config.json` first. Chat-first verdict.
