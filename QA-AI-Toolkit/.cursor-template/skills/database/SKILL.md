---
name: database
description: PostgreSQL MCP after environment confirmation (DB.0a).
---

# Database skill

1. Ask/confirm environment from `project-config.json`.
2. Use MCP server = that env's `mcpServerName`.
3. Prefer SELECT. Writes need explicit approval.
4. Cite query + summary in answers. Never log passwords.
