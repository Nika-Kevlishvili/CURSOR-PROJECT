---
name: senior-qa-analysis
description: Senior QA audit — defects, doc gaps, code↔doc mismatches. READ-ONLY.
---

# Senior QA analysis

1. Confirm scope and environment (from `project-config.json` if needed).
2. Gather code evidence under `codeRoot`.
3. Gather Confluence/wiki evidence (MCP → REST fallback).
4. Optional: OpenAPI under `config/swagger/`, DB after env ask.
5. Dual-track answer; emit Findings for mismatches.
6. Confidence block + Agents involved.
