# Reports layout

Persisted reports only when the user runs `/report`, `/feedback`, or explicitly asks to save.

```
reports/
  Chat reports/YYYY/<english-month>/<DD>/
  Feedback/YYYY/<english-month>/<DD>/
```

See `.cursor/rules/main/core_rules.mdc` Rule 0.6.
