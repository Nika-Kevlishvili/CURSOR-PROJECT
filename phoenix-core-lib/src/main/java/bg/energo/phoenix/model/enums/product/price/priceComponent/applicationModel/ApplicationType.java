package bg.energo.phoenix.model.enums.product.price.priceComponent.applicationModel;

public enum ApplicationType {
    BY_SETTLEMENT_PERIODS, BY_SCALES, PERIODICALLY, ONE_TIME, WITH_ELECTRICITY_INVOICE
}
