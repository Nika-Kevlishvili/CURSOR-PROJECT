{
  "description": "Canonical Swagger UI and OpenAPI JSON URLs per environment. Edit this file when hosts change. Fetched by .cursor/commands/update-swagger-specs.ps1 into <id>/swagger-spec.json via openapi_json. Replace the <...> placeholders with your project's real hosts.",
  "environments": [
    {
      "id": "dev",
      "swagger_ui": "http://<DEV_HOST>:<PORT>/swagger-ui/index.html#",
      "openapi_json": "http://<DEV_HOST>:<PORT>/v3/api-docs"
    },
    {
      "id": "test",
      "swagger_ui": "http://<TEST_HOST>:<PORT>/swagger-ui/index.html#",
      "openapi_json": "http://<TEST_HOST>:<PORT>/v3/api-docs"
    },
    {
      "id": "prod",
      "swagger_ui": "http://<PROD_HOST>:<PORT>/swagger-ui/index.html#",
      "openapi_json": "http://<PROD_HOST>:<PORT>/v3/api-docs"
    }
  ]
}
