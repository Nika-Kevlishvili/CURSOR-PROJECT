User Story 
As a system user
I want to be able to update product contract data to its current version or create a new version
So that I have possibility to correct and to update product contract information

Acceptance Criteria 
Check system user permissions to see edit icon on product contract listing/product contract detail page, if no such permission, edit icon will be disabled 
Editing product contract permissions
Draft product contracts can be edited

Ready product contracts can be edited with additional permission – edit ready product contracts

All product contracts can be edited with additional permission – edit all product contracts if they are not locked (invoice is not created)
Draft, ready, signed, enter into force, active in terms, active in perpetuity, terminated, cancelled

All locked product contracts (contracts with created invoices) can be edited with additional permission – edit locked product contracts

In all product contracts status can be edited with additional permission – edit product contracts statuses (it gives permission to edit only status and sub-status fields and version status field)

Product contracts can be saved without RiskList check when user has additional permission – Skip RiskList check.

Clicking on an edit icon in product contract listing or in product contract detail page will allow product contract edit mode

By default system will load currently active contract’s version  

Version’s dropdown in bottom right side will allow to display other product contracts versions and to update them. System should display version with format:
Version number

Version start date

Version end date (if there is no end date should display open end)

E.g. 18 / 15.04.2021 - 30.04.2022 or 19 / 01.05.2022 – open end

Dropdown should be ordered by start date

Some fields can not be edited in Product contract so in editing mode fields should be disabled
Creation Date
Disabled

Should display contract creation date - automatically generated by the system after creating the contract

Contract’s creation date will be the same for all version

Contract number
Disabled

Should display contract number - automatically generated by the system and will be the same for all version

Additional suffix
When editing Contract with Type 'Additional Agreement' OR Ex officio agreement to generate additional contract suffix number. Counting will start from #1 and should be unique under one contract
Contract suffix number should be displayed next to contract number in preview/edit:

if Contract has more than one version with type 'Additional Agreement' OR Ex officio agreement' should have unique numbers:  #1, #2, #3  and etc

If in contract V1 the type was 'Additional Agreement' and additional number was #1 and then I changed type as Contract  
Should hide #1 in contract 1st tab

When create a new version V2 should take next available number, #2 in this case. 

If contract V1 is changed again and set again type 'Additional Agreement'  in this case should be visible #1 which was previously saved.  

Date of activation
Disabled

Connected to contract, not version, in editing will be changed in all versions

Some fields can be edited in Product contract so in editing mode fields should be enabled
Product/Individual product
Mandatory

When Product or its version is changed and saved as a current version, all the records saved in 3rd product parameters tab should be cleared (+ to remove in DB) and newly added product’s parameters should be saved

In the dropdown should display only products available for Sales channels of the user. If user is not connected to any sales channel, he/she should be able to see all products (Login portal integration will be possible in next bundles)

If individual product is added to contract and contract’s new version is created, individual product should be copied and newly created individual product should be linked to a new version.

One individual product can be linked to only one contract’s version.

Standard product version can be linked to more than one contract, so when creating new contract version with standard product the product version shouldn’t be copied.

If product which is saved in contract is not available any more in editing still can be saved in contract. for all other products same filtering of products should be used which exists in creation   

While editing if product is changed and new product has different configuration, new configuration should be considered

Customer
Mandatory

On editing additionally always should be checked customer’s billing groups. If customer has more than one billing group with same identifier then should display an error and shouldn’t allow to save change 

When customer / customer version is removed from contract, communication data for billing/contract , proxy should be also removed

Communication data
Mandatory

Can be edited.

If from customer’s version communication data was removed after concluding contract, in editing user must select new communication data in order to save the changes.

Proxy
Optional

Can be edited.

If from customer’s version communication data was removed after concluding contract, in editing user must select new communication data in order to save the changes.

Signed from/to
Sub-objects should be always disabled and contract number should be displayed according to the Resigning process

Contract files
Optional

Should be linked to contract with information for version in which are added/created

Generated from the system files can not be removed

Additional documents
Optional

Should be linked to contract with information for version in which are added/created

Related contracts/orders
Optional

Linked to contract, not version. So, in editing related contract will be changed in all versions.

Signing date
Disabled if status is Draft, Ready, Signed (signed by customer, signed by EPRES)

 When the user has only Create Product Contracts, Edit Product Contracts ready, or Edit Product Contracts draft permission, Signing date is disabled

Optional and enabled when contract status is Active in terms, Active in perpetuity, Entered into force

Mandatory and enabled when Date of activation, Entering info force date, Contract term end date, Perpetuity date, Date of termination is added. Additionally, when contract status is Signed (signed by both sides, special processes)

Less or equal: Entering into force, start of the initial term, date of activation, supply activation date(3rd tab, if not empty)

Less: termination, perpetuity, contract term end date

Connected to contract, not version, in editing if Signing date will be changed in all versions.

If signing date is filled then to check 3rd tab settings and if 'From signing' is selected should save the date in fields:
Start of the initial term of the contract
Then to check if selected Contract term end date is with type 'Period' and should calculate and set contract term end date as well

e.g. if start of the initial term is 1st May and contract term’s period is 3 months, end date should be 1st August

Entry into force date

During editing if start date or Entry into force date is added manually from 1st tab fields and also Signing date is filled system should ignore automatic calculation and should set  

Entry into force date
Disabled and empty if contract status is Draft

Optional if
Ready, signed, cancelled

Mandatory if
Entered into force, active in terms, active in perpetuity, terminated

More or equal then signing date

Less or equal then end date, termination date, perpetuity

To be able to change manually already added /calculated value

If in the third tab, in the Product Parameters Manual option is selected in the Entered into force of the contract (Entry into force date), such defined date shouldn't be displayed until contract saving. After contract is saved, relevant dates should be displayed in the first tab and it is possible for them to be in the future.
If contract status stays Draft after saving, such dates shouldn’t be displayed in the first tab

If contract status is changed to Ready, after its saving relevant dates should be displayed in the first tab

If in the third tab, in the Product Parameters From Exact Day option is selected in the Entered into force of the contract (Entry into force date), such defined date shouldn’t be displayed until contract saving. After contract is saved, relevant dates should be displayed in the first tab and it is possible for them to be in the future.
If contract status stays Draft after saving, such dates shouldn’t be displayed in the first tab

If contract status is changed to Ready, after its saving relevant dates should be displayed in the first tab

If the settings is First delivery or date of change CBG should set status automatically and can be rewritten already calculated value

if the settings is From signing  and Entry into force date is filled shouldn’t replace already filled value

When in contracts 3rd tab settings are changed system should check if value is added or not (no matter is it manually added or calculated by system)
If value is not filled then system should calculate value based on new configuration)

If value is already filled then system should not affect already calculated value

When user defined Entry into Force date in the past/current day in the first tab of the Product Contract, it is mandatory to change contract status as Entered into Force

When user defined Entry into Force date in the future in the first tab of the Product Contract, contract status can be Ready or Signed

Connected to contract, not version, in editing will be changed in all versions.

Termination date
Disabled if status is not terminated 

Editable and mandatory  if contract status is terminated 
date should be past 

More than signing date

More or equal to entry into force date(if exists), start date (if exists), date of activation (if exists), perpetuity date (if exists)

Connected to contract, not version, in editing will be changed in all versions.

Contract term start date
Disabled and empty if status is Draft

Enabled and optional for other statuses

To be able to change manually already added /calculated value

To be able to set dates in range 01.01.1990 - 31.12.2090

More or equal than signing date, entry into force date(if exists)

Less or equal than entry into force date(if exists), date of activation (if exists) 

Less than contract term end date, perpetuity date (if exists)

When in contracts 3rd tab settings are changed system should check if value is added or not (no matter is it manually added or calculated by system)
If value is not filled then system should calculate value based on new configuration)

If value is already filled then system should not affect already calculated value

If in the third tab, in the Product Parameters Manual option is selected in the Start of the initial term of the contract (Contract term start date), such defined date shouldn't be displayed until contract saving. After contract is saved, relevant dates should be displayed in the first tab and it is possible for them to be in the future.
If contract status stays Draft after saving, such dates shouldn’t be displayed in the first tab

If contract status is changed to Ready, after its saving relevant dates should be displayed in the first tab

If in the third tab, in the Product Parameters From Exact Day option is selected in the Start of the initial term of the contract (Contract term start date), such defined date shouldn’t be displayed until contract saving. After contract is saved, relevant dates should be displayed in the first tab and it is possible for them to be in the future.
If contract status stays Draft after saving, such dates shouldn’t be displayed in the first tab

If contract status is changed to Ready, after its saving relevant dates should be displayed in the first tab

Connected to contract, not version, in editing will be changed in all versions.

Contract term end date 
Disabled when status is Draft

Enabled and optional for other statuses

To be able to change manually already added /calculated value

When in contracts 3rd tab settings are changed system should check if value is added or not (no matter is it manually added or calculated by system)
If value is not filled then system should calculate value based on new configuration)

If value is already filled then system should not affect already calculated value

To be able to set dates in range 01.01.1990 - 31.12.2090

More than signing date, entry into force date(if exists), start date (if exists), date of activation (if exists), perpetuity date (if exists)

Connected to contract, not version, in editing will be changed in all versions.

Perpetuity date
Enabled and mandatory if status is Active in perpetuity
Date is less or equal than current day

Disabled in all other cases

More then signing date, entering into force date, contract term start date

Less then contract term end date, termination date

Connected to contract, not version, in editing will be changed in all versions

Contract term until amount is reached
Optional

Editable

Contract term until volume is reached
Optional

Editable

Contract’s status and sub-statuses:
Linked to contract, not version.

After selecting contract’s status, sub statuses should be filtered accordingly.

When status is 'draft' contract status can be changed as:
Ready (manually),

Entered into force (manually)

Signed (manually)

Cancelled (manually)

When status is 'ready' contract status can be changed as
Draft (manually)

Signed (manually)

Entered into force (manually)

Cancelled (manually):

When contract is ‘signed’ contract status can be changed as:
Entered into force ( manually, automatically)

Terminated (Applicable only if signed by both sides)

Cancelled (manually) – applicable if status is Signed and sub-status is Signed by customer, Signed by EPES, special process

When contract is ‘entered into force’ contract status can be changed as:
Active in terms ( automatically )

Terminated ( manually, automatically)

When contract is ‘active in terms’ contract status can be changed as:
Active in perpetuity ( manually, automatically)

Terminated ( manually, automatically)

When contract is ‘active in perpetuity’ contract status can be changed as:
Terminated ( manually, automatically)

When contract is ‘terminated’ contract status can be changed as:
Active in terms ( manually)

Active in perpetuity ( manually)

When contract is ‘cancelled’ contract status can not be changed

When the user has only Create Product Contracts, Edit Product Contracts ready, or Edit Product Contracts draft permission, the Contract status picker does not offer parent statuses Signed or Entered into force, and sub-status options tied to those parent statuses are not shown wherever sub-status is displayed.

Contract version status
Frontend

For contract Version status frontend validation should be removed at all. and all statuses is dropdown should be always visible:

Draft
Ready

Signed

Cancelled

If some error happens in backend should display error in field

Backend logic
In backend should be validated according this logic and if some error happens should display error in frontend:

When contract version status is Draft it can be changed as:
Ready (manually)

Signed (manually)

Cancelled (manually)

When contract version status is Ready it can be changed as:
Draft (manually)

Signed (manually)

Cancelled (manually)

When contract version status is Signed it can be changed as:

Draft (manually)

Ready (manually)

Cancelled (manually)

When contract version status is Cancelled it can be changed as:

Draft (manually)

Ready (manually)

Signed (manually)

In new contract version creation should not have any restrictions for the contract version status

Contract version type
Editable fields

Connected to version

Customer Deal number
Can be added manually or automatically after xEnergie integration

If deal number is set manually or automatically, deal number can be changed (manually or via xEnergie integration)

Deal number is connected to version

Estimated total consumption
Disabled and calculated automatically
sum of (Estimated monthly average consumption * 12 )/1000

Connected to contracts version

Risk assessment
Disabled

In editing as current version/new version always should be checked risk assessment status. If denied or missed editing shouldn’t be possible. Should display reason in risk assessment field

If current user has Skip RiskList check permission, RiskList API check should be skipped before save as new version and save as existing version.

Connected to contracts version

Risk assessment additional conditions
Disabled

Connected to contracts version

Direct debit, Bank, BIC , Bank account
Editable fields

Connected to version

Applicable interest rate
Editable

Connected to contracts version

Campaign
Connected to contracts version

Activity
Separate editing.

It is possible to add activities to the product contract by clicking on a Plus button on the activity's sub-object

Permission - Create Activity from Contract
This permission grants the ability to create an Activity in the Preview/Edit mode.
In the preview mode, the Activity Create button should be enabled.

If the user does not have Contract edit permission, it is only possible to create a task in the Preview mode

Adding activity to the product contract is optional

Adding Activity to the product contract is possible only on the editing mode
On the product contract creation mode, it is not possible to add activity and its sub-object should be disabled

Clicking on a Plus button will open new browser window of the activity's creation page
When the activity's creation page is opened from the product contract, the contract should be displayed in the sub-object automatically

After creating an activity, it will be automatically added to the product contract

Activity should be added with the following format: Activity-Sub-activity

It is possible to add more than one activity to the product contract

After adding activity to the product contract, it is not possible to delete it

After adding activity to the product contract, it is possible to go to the activity's detailed page when clicking on its name or an arrow
Open activity's detailed page in a new tab

Connected to contract, not its version

Task
Separate editing.

It is possible to add tasks to the contract by clicking on a Plus button on the task's sub-object

In order to create a task from the contract, user should have additional permission: Permission - Create Task from contract
This permission grants the ability to create tasks in the Preview

In the preview/edit mode, the Task button should be enabled.

If the user does not have contract edit permission, it is only possible to create a task in the Preview mode.

Adding task to Product Contract is optional

Clicking on a Plus button will open new browser window of the task's creation page
If the task creation page is open from the product contract , that contract should be displayed in Contract/Order sub-object in the task automatically

If the task creation page is open from the product contract page, Contract/Order value should be selected automatically in the Connection Type and it is not possible to change it

After creating a task and refresh its sub-object, it will be automatically added to the contract

Task should be added with the following format: Task number-Creation date-Task type

It is possible to add more than one task to the contract

After adding task to the contract, It is possible to go to the task's detail page when clicking on its name or an arrow
Open task's detailed page in a new tab

Connected to contract, not its version

Employee
Editable

Connected to version

List is taken from system users (same integration what we have for account managers)

When user has permission – Edit Product Contracts Additional agreements, when entering Edit mode system should update Employee (Contract's Additional Parameters tab) with the current user.

Assisting employee
Editable

connected to version

Internal intermediary
Editable

connected to version

External intermediary
Editable

connected to version

Product parameters fields
For each product field all available options should be taken from product and should be editable

If the value which was saved in contract is removed from product, in editing that value shouldn’t be visible and all other options should be visible and unselected. If user doesn’t select the value should return an error. After saving changes useless values from old products parameters saved in contract should be removed

Billing groups
Separate editing

In contract editing mode groups section will be listed all groups which is connected to that contract. 

In contract editing will be possible to edit group’s details or create a new group:
a. For editing a billing group, user should have permission: ‘Edit Billing Group’
b. For creating a billing group, user should have permission: ‘Create Billing Group’

Clicking on a group will open groups detail page in preview mode. From that page will be possible to click on 'Edit' icon and start editing.

Fields:
Group number - editable

Sending the invoice - editable

Separate invoice for each point of delivery - editable

Direct debit - editable

Alternate invoice recipient - editable

Clicking on Save button will check all the validations 
If frontend validations are successful saving Billing group request should be sent in backend and billing group should be saved in the contract’s all versions

Activate /Deactivate PODs
Activation/Deactivation button should be disabled if POD is not saved in contract in DB (Only added in frontend) or haven’t permission “Point of delivery manual activation/deactivation”

Enabled if POD is saved to contract in DB and have permission “Point of delivery manual activation/deactivation”

Clicking on icon will open Modal window for adding removing activation date

Fields added in modal window:
Point of delivery
Display PODs identifier

Disabled and can’t be changed

Activation Date
Less or equal than deactivation date

Deactivation Date
More or equal than activation date 

Deactivation reason
Single Select dropdown

List of deactivation reason is manageable from nomenclature

Display Search Bar if number of items are more than 10 items in dropdown list

While typing search letter list in dropdown should be filtered by search letters

Display default value if returned from nomenclature

When POD is deactivated from defined process, hardcoded value should be set in the field - “Activated in new version”. 
this value should be set in deactivation reason after automatic POD deactivation from
manual/mass import

contract’s version start date changing

contract new version creation

resigning flow

contract current version creation (from customer new version)

Before set or replace activation date in POD should check contract signing date and it should be less or equal then pod activation date

when adding POD in contract should check if this POD has activation date without deactivation date in previous version and set deactivation date as a last day before new version start date.

If this POD doesn’t exist in previous version but exist in another old version shouldn’t do anything even it has only activation date.

When imported/selected activation date is more then current date shouldn’t update contract status.

When removing POD from product contract’s version, if POD has activation date in any future version system shouldn’t remove this POD from future version.
Display error: “POD can’t be removed, v3 should include at least 1 POD”

when adding POD in contract should check if this POD has activation date without deactivation date in previous version and set deactivation date as a last day before new version start date.

If this POD doesn’t exist in previous version but exist in another old version shouldn’t do anything even it has only activation date.

It is not possible to remove POD from the contract’s version if it has activation date defined

Save button – when user adds the data clicks on save button
Validations should be checked in frontend /backend
Activation Date is empty and deactivation date isn’t empty
Display error “Activation date shouldn’t be empty”

Activation and deactivation date less than contract version's start date
Display error “Activation/deactivation date shouldn’t be less than contract version’s start date”

Activation and deactivation date more than contract version's end date
Display error “Activation/deactivation date shouldn’t be more than contract version’s end date”

Deactivation date is empty and deactivation reason isn’t empty
Display error “Deactivation date shouldn’t be empty”

Deactivation date isn’t empty and deactivation reason is empty
Display error “Deactivation reason shouldn’t be empty”

If not successful – Error messages

if successful saving will be performed in DB
modal window should be closed and updated activation/deactivation dates will be displayed in the POD list

Cancel button – modal window is closed and user stays on listing page
If in modal window there is unsaved changes should display confirmation message 'Are you sure you want to close?' If yes then modal window will be closed. system message is configurable

After starting process for each POD system should check if POD can be activated/deactivated under some contract.
https://app.diagrams.net/?state=%7B%22ids%22:%5B%221kz-Ho6gUroE3YgbchcxtQpjD0phLal2r%22%5D,%22action%22:%22open%22,%22userId%22:%22103968274867563528104%22,%22resourceKeys%22:%7B%7D%7D
PODs
POD quantity limit should be 5000

To be able to add/remove POD in editing process

To be able to change billing groups in contract editing process

Connected to version

If a POD is added, after saving it should be added automatically in all future versions of the contract in the same billing group
if POD is already added in some future versions with different PODs version in this case POD will not be updated in this version and all other future versions will be updated 

If POD is added in previous version with activation and without deactivation date, after saving it  system should automatically set deactivation date in previous version as a last day before new version start date

If this POD doesn’t exist in previous version but exist in another old version shouldn’t do anything even it has only activation date.

 when create new  last version including POD from previous version system should check deactivation date in previous version
if deactivation date is empty system should set deactivation date in previous version as a last day before new version start date and set activation date in new version as a new version start date

if deactivation date is less then new version start date, system shouldn’t set  activation date in new version as a new version start date

if deactivation date is more or equal then new version start date, system should update deactivation date in previous version as a last day before new version start date and set activation date in new version as a new version start date and set deactivation date in new version from  previous version deactivation date

Remove selected button 
Disable if at least one point of delivery isn’t marked.

Enable if any of point of delivery is marked

If a POD is removed, after saving 
Display error if POD has activation date 

If POD hasn’t activation date continues process 

if POD hasn’t activation date in future version, it should be removed automatically from all future versions of the contract

If POD has activation date in future version, it shouldn’t be removed automatically from future version

If activation /deactivation dates is added in some future version then will not be removed 

when create new version without POD from previous version system should check deactivation date in previous version
if deactivation date is empty system should set deactivation date in previous version as a last day before new version start date

if deactivation date is less then new version start date, system shouldn’t change activation/deactivation dates

if deactivation date is more or equal then new version start date, system should display error “new versions start date is less then some of PODs deactivation date in previous version”

When new POD is added in contract (in current version or new version is created) system should check all the validations that exists in contract creation. List of newly added PODs should be matched with configuration in products version. Product parameters which should be checked:
Purpose of consumption 

Metering type of the points of delivery

Voltage level

Grid operator

If already added POD’s configuration is changed in this case system shouldn’t check any parameters from product and should allow saving as a current version/create new version. 
Additionally, when POD is added automatically in contracts future versions in this case it shouldn’t be validated in future contract’s versions settings. So if validation in  existing version is passed in all future version will be always added  

When creating new version(it doesn’t matter this is last or middle) should check if POD are included from previous version
previous version is last version which start date is less then new version start date
If yes, after saving should check deactivation date in previous version
if deactivation date is empty system should set deactivation date in previous version as a last day before new version start date and set activation date in new version as a new version start date

if deactivation date is less then new version start date, system shouldn’t set activation date in new version as a new version start date

if deactivation date is more or equal then new version start date, system should update deactivation date in previous version as a last day before new version start date and set activation date in new version as a new version start date and set deactivation date in new version from previous version deactivation date

If no, after saving should check deactivation date in previous version
if deactivation date is empty system should set deactivation date in previous version as a last day before new version start date

if deactivation date is less then new version start date, system shouldn’t change activation/deactivation dates

if deactivation date is more or equal then new version start date, system should display error “POD can’t be activated in new version ”

During Product Contract creating/editing flow it is mandatory to check PODs according to its measurement type:
If in the product only By settlement period is selected in the metering type of the point of delivery, it means that it is possible to add only those PODs to the contract which has the same measurement type selected (by settlement period)

If in the product only SLP is selected in the metering type of the point of delivery, it means that it is possible to add only those PODs to the contract which has the nomenclature value selected from the measurement type nomenclature and it is different from By settlement periods options.

If both options are selected in the product (By settlement period and SLP), it means that it is possible to add all the PODs to the product contract.

Create new individual Product when creating new version
If individual product is added to contract and contract’s new version is created, individual product should be copied and newly created individual product should be linked to a new version

One individual product can be linked to only one contract’s version

System should not check individual product’s status (Active or Inactive) while cloning it 

New objects should be created and linked to newly created individual product (no need to additional check -for active/inactive values):
Terms

Interim and advance payment (if advance payment contains price component, then new price component should be also created)  

Penalty

Termination

Price component

Already existing groups should be linked to new individual product
Group of Terms

Group of Interim and advance payment 

Group of Penalty

Group of Termination

Group of Price component

Files should be linked to newly created individual product

All other product related fields should be copied in newly created individual product

Generate button
For terminated/canceled contract is not active generate button

For draft Generate button should be enabled but generated documents should be with watermark DRAFT

For all other statuses Generate button should be enabled

It is possible to 'Update existing version' or to ‘Create new version’ 

Clicking on a button 'Update existing version' will update existing contract version 
Check all validations and if successful will update existing version

Clicking on a button ‘Create new version’ will check all validations in frontend. If successful will display pop-up to add version validity start date, else error
It is required start date to be unique for versions of this contract (one contract cannot have more than one version active in one day)

it is possible to create version with an old dates

If sets start date which is already added, will display warning message

It is possible to have more than one new versions

It is not possible to delete already created version

After adding start date and clicking on 'save' button will create new contract version

After creating new version system automatically should update the dates for POD activation deactivation

RiskList check skipping by Skip RiskList check permission should apply for both save paths: update existing version and create new version.

Audit Log record should be created on editing process 

Status
Sub-status

Draft
Draft

Ready
Ready

Signed
Signed by customer

Signed by the EPRES

Signed by both sides

Special processes

Entered into force
Awaiting activation

Active in term
Delivery

In termination by customer

In termination by EPRES

In termination by GO data

Active in perpetuity
Delivery

In termination by customer

In termination by EPRES

In termination by GO data

Terminated
From customer with notice

From customer without notice

From EPRES with notice

From EPRES without notice

By mutual agreement

New contract signed

Expired

Deleted ID / Deceased person

Force majeure

All POD deactivated

Contract is not activated in terms

Cancelled
Unsent to customer

Invalid data

Refusal to sign by customer

Refusal to sign by EPRES

Test

Customer Feedbacks: https://oppa-support.atlassian.net/browse/PDT-2663 
https://oppa-support.atlassian.net/browse/PDT-2852 
https://oppa-support.atlassian.net/browse/PDT-2637 
https://oppa-support.atlassian.net/browse/PDT-2001?search_id=8036f728-f18e-48fc-9997-cc4ccc676d1d