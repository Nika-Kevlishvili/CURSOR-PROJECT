# Confidence Scoring Matrix

**Canonical location:** `.cursor/rules/scoring/confidence_scoring_matrix.mdc` (auto-loaded by Cursor when SKILL/agent/workflow files are active).

This file is a reference copy. The `.mdc` version under `.cursor/rules/scoring/` is the authoritative source with `alwaysApply: false` + glob triggers.

See the `.mdc` file for full content: factor tables, Three-Zone routing, Prod safety gate, output formats, aggregation methods, calibration logging, and recalibration instructions.
