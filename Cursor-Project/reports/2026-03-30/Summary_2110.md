## Summary

- **Fixed** `shell` Task subagent by adding `.cursor/agents/shell.md` and documenting it in README + docs + agent_rules Rule 13.
- **Verified:** `subagent_type: shell` smoke test returned `OK-shell-fixed`.

## Agents involved

PhoenixExpert (coordination), shell subagent (verification).
