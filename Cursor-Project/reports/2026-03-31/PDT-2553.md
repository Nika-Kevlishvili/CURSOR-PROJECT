PDT-2553 – Playwright test results

Jira: PDT-2553
Title: When send email to more than one recipient, to send one email with multiple recipients not many separate emails
Date: 2026-03-31
Assignee: "Mariam Gogoladze" / Tester: "nika kevlishvili" .

Total: 0 passed, 0 failed, 5 skipped.

-------------------------

Playwright test results:

Test 1: [PDT-2553] Send email to a single recipient – baseline behaviour (TC-1)

Test description:
Baseline single-recipient behaviour; this test would verify that exactly one communication with a single linked contact is recorded with successful status and that this baseline remains valid when multi-recipient behaviour is introduced.

Expected result:
One communication exists with a single linked contact marked as SENT/SUCCESS and no duplicate sends.

Actual result:
Test not run in this execution (marked as Skipped in Playwright run; no HTTP calls/assertions executed).

Test result:
Not run


Test 2: [PDT-2553] Send email to two recipients – one email with multiple recipients (TC-2)

Test description:
Two-recipient scenario; this test would ensure that when two recipients are selected, the system sends a single email communication that covers both recipients and records one communication with two successful contacts.

Expected result:
Single communication with two successful contacts; no duplicated communications or per-contact sends.

Actual result:
Test not run in this execution (marked as Skipped in Playwright run; no HTTP calls/assertions executed).

Test result:
Not run


Test 3: [PDT-2553] Send email to many recipients across multiple customers – single email with combined recipients (TC-3)

Test description:
Multi-customer multi-recipient scenario; this test would cover sending a communication to three recipients across multiple customers and validate that the system still behaves as a single outbound email covering all recipients, with correct linkage and statuses.

Expected result:
One communication with appropriate customer/contact relations and all contacts in success status; no extra per-customer mails.

Actual result:
Test not run in this execution (marked as Skipped in Playwright run; no HTTP calls/assertions executed).

Test result:
Not run


Test 4: [PDT-2553] Resend previously sent email communication – single resend message with same recipient set (TC-4)

Test description:
Resend scenario; this test would verify that when resending an existing communication, exactly one new resend attempt is recorded with the same recipient set and without creating duplicate communications.

Expected result:
Resend tracking shows one additional send; recipient set unchanged; no duplicate communications.

Actual result:
Test not run in this execution (marked as Skipped in Playwright run; no HTTP calls/assertions executed).

Test result:
Not run


Test 5: [PDT-2553] Attempt to send email with no recipients selected (TC-5)

Test description:
Validation scenario for empty recipient selection; this test would attempt to trigger send with no recipients selected and assert that the API returns a clear validation error and that no sendable communication is persisted.

Expected result:
Validation error response (e.g. HTTP 400 with an appropriate message); no communication created in SENT/PENDING state.

Actual result:
Test not run in this execution (marked as Skipped in Playwright run; no HTTP calls/assertions executed).

Test result:
Not run

