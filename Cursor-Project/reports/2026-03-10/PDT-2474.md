# PDT-2474 – Playwright test results

**Jira:** PDT-2474  
**Title:** Liabilities and receivables shouldn't be generated with amount zero.  
**Date:** 2026-03-10  
**Assignee:** Mariam Gogoladze  
**Tester:** ana zarkua  

**Total:** 0 passed, 4 failed, 5 skipped.

**Spec:** `EnergoTS/tests/cursor/PDT-2474-zero-amount-liability-receivable.spec.ts`  
**Run:** `npx playwright test --grep "PDT-2474"` or `npx playwright test tests/cursor/PDT-2474-zero-amount-liability-receivable.spec.ts`

---

## Playwright test results

### Test 1: [PDT-2474] TC-1: Manual liability creation rejected when initial amount is zero

**Test description:**  
Create customer via API, then POST manual liability with initialAmount 0; expect validation error (400). Verifies that the system rejects manual liability creation when initial amount is zero.

**Expected result:**  
HTTP 400; no CustomerLiability with initialAmount zero created.

**Actual result:**  
Request to API host (10.236.20.11:8091) timed out (ETIMEDOUT). Test could not reach the customer creation endpoint.

**Test result:** Failed

---

### Test 2: [PDT-2474] TC-2: Manual receivable creation rejected when initial amount is zero

**Test description:**  
Create customer via API, then POST manual receivable with initialAmount 0; expect validation error (400). Verifies that the system rejects manual receivable creation when initial amount is zero.

**Expected result:**  
HTTP 400; no CustomerReceivable with initialAmount zero created.

**Actual result:**  
Request to API host (10.236.20.11:8091) timed out (ETIMEDOUT). Test could not reach the customer creation endpoint.

**Test result:** Failed

---

### Test 3: [PDT-2474] TC-3: Manual liability creation succeeds when initial amount is non-zero

**Test description:**  
Create customer via API, then POST manual liability with initialAmount 10; expect success and created entity with initialAmount 10. Verifies happy path for manual liability creation.

**Expected result:**  
HTTP 2xx; CustomerLiability created with initialAmount 10.

**Actual result:**  
Request to API host (10.236.20.11:8091) timed out (ETIMEDOUT). Test could not reach the customer creation endpoint.

**Test result:** Failed

---

### Test 4: [PDT-2474] TC-4: Manual receivable creation succeeds when initial amount is non-zero

**Test description:**  
Create customer via API, then POST manual receivable with initialAmount 25.5; expect success and created entity with initialAmount 25.5. Verifies happy path for manual receivable creation.

**Expected result:**  
HTTP 2xx; CustomerReceivable created with initialAmount 25.5.

**Actual result:**  
Request to API host (10.236.20.11:8091) timed out (ETIMEDOUT). Test could not reach the customer creation endpoint.

**Test result:** Failed

---

### Test 5: [PDT-2474] TC-5: Billing run does not persist liability/receivable when computed amount is zero

**Test description:**  
Billing run (or start-accounting) must not persist CustomerLiability/CustomerReceivable when computed amount is zero. Skipped: requires full contract/POD/billing setup; no single API.

**Expected result:**  
No zero-amount liability/receivable persisted from billing run.

**Actual result:**  
Skipped (no simple API; covered by integration/e2e).

**Test result:** Skipped

---

### Test 6: [PDT-2474] TC-6: Invoice cancellation does not persist zero-amount liability or receivable

**Test description:**  
Invoice cancellation must not persist zero-amount liability/receivable. Skipped: requires invoice and cancellation scenario and DB check.

**Expected result:**  
No zero-amount liability/receivable created by invoice cancellation.

**Actual result:**  
Skipped (requires invoice + cancellation scenario).

**Test result:** Skipped

---

### Test 7: [PDT-2474] TC-7: Invoice reversal does not persist zero-amount liability or receivable

**Test description:**  
Invoice reversal must not persist zero-amount liability/receivable. Skipped: requires invoice and reversal flow.

**Expected result:**  
No zero-amount liability/receivable created by invoice reversal.

**Actual result:**  
Skipped (requires invoice + reversal flow).

**Test result:** Skipped

---

### Test 8: [PDT-2474] TC-8: Persistence layer rejects zero amount (e.g. JPA @PrePersist)

**Test description:**  
Persistence layer (ZeroAmountValidationListener) must reject persist of CustomerLiability/CustomerReceivable with initialAmount zero. Skipped: not testable via REST API; use unit/integration test in backend.

**Expected result:**  
Zero-amount persist attempt rejected at persistence layer.

**Actual result:**  
Skipped (persistence-layer; not REST API).

**Test result:** Skipped

---

### Test 9: [PDT-2474] TC-9: Billing run credit note or correction does not persist zero-amount receivable

**Test description:**  
Billing run credit note/correction must not persist zero-amount receivable. Skipped: requires full billing setup; no single API.

**Expected result:**  
No zero-amount receivable persisted from credit note/correction flow.

**Actual result:**  
Skipped (billing sub-flow; no simple API).

**Test result:** Skipped

---

## Summary

- **4 tests failed** due to API host unreachable (10.236.20.11:8091 – ETIMEDOUT). Run tests in an environment with network access to the API (e.g. CI/GitHub or VPN).
- **5 tests skipped** by design (billing/invoice/reversal/persistence scenarios with no simple REST API; to be covered by integration/e2e or backend unit tests).
