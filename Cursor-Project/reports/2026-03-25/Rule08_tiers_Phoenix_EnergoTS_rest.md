# Rule 0.8 update — three-tier paths (2026-03-25)

**User policy:**

1. **Phoenix (`Cursor-Project/Phoenix/**`):** No Cursor AI modifications under any circumstances (read/analyze only).
2. **EnergoTS:** Only **EnergoTSTestAgent** (+ adapter) may write under `Cursor-Project/EnergoTS/tests/**`. No other AI writes anywhere under `Cursor-Project/EnergoTS/**` (including tests) from generic tool usage.
3. **Everything else:** Edits allowed when the user requests (**provisional**).

**Files updated:** `.cursor/rules/main/core_rules.mdc` (Rule 0.8, 0.8.1), `.cursor/rules/safety/safety_rules.mdc` (Rules 7, 31), `.cursor/rules/main/phoenix.mdc` (quick reference).

**Supersedes:** Narrower “ALLOWED paths only” policy from earlier same-day edit (`Rule08_scoped_product_vs_agents.md`).

Agents involved: None (rules edit)
