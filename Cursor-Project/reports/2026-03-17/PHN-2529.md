# PHN-2529 – Playwright test results

**Jira:** PHN-2529  
**Title:** Get customer list by sales agent coordinates  
**Date:** 2026-03-17  
**Assignee:** Paata Khachidze / **Tester:** N/A  

**Total:** 1 passed, 7 failed, 0 skipped.

**Spec:** `EnergoTS/tests/cursor/PHN-2529-sales-portal-customers-by-coordinates.spec.ts`  
**Run:** `npx playwright test tests/cursor/PHN-2529-sales-portal-customers-by-coordinates.spec.ts`

-------------------------

## Playwright test results

### Test 1: [PHN-2529] Valid request with coordinates and pagination returns success

**Test description:**  
Call GET /sales-portal/customers/by-coordinates with valid coordinates (lat=41.7, lon=44.8) and pagination (page=0, size=10) using valid JWT. Assert response status 200 and body is JSON with expected structure.

**Expected result:**  
Request succeeds (HTTP 200). Response body is valid JSON and matches schema (content array, pagination metadata). No server error or 401/403.

**Actual result:**  
Response was not OK (res.ok() false). Likely 401 Unauthorized or 404 Not Found—endpoint may not be available on the configured BASE_URL or token not accepted for Sales Portal API.

**Test result:** Failed

---

### Test 2: [PHN-2529] Invalid or missing coordinate parameters are rejected

**Test description:**  
Call GET /sales-portal/customers/by-coordinates with invalid lat (lat=abc). Expect client validation error.

**Expected result:**  
HTTP 400 with clear validation message.

**Actual result:**  
Received HTTP 404 instead of 400. Endpoint may not exist at the requested URL, so validation was not reached.

**Test result:** Failed

---

### Test 3: [PHN-2529] Missing or invalid authentication is rejected

**Test description:**  
Call GET /sales-portal/customers/by-coordinates without Authorization header. Expect 401 or 403.

**Expected result:**  
401 Unauthorized or 403 Forbidden; no customer data returned.

**Actual result:**  
Request rejected with 401 or 403 as expected.

**Test result:** Passed

---

### Test 4: [PHN-2529] Customers returned when coordinates match existing data

**Test description:**  
Call GET /sales-portal/customers/by-coordinates with coordinates that cover an area containing customers. Verify response includes customer list.

**Expected result:**  
HTTP 200 with non-empty list and pagination metadata.

**Actual result:**  
Response was not OK (res.ok() false). Same environment/token/URL issue as Test 1.

**Test result:** Failed

---

### Test 5: [PHN-2529] Pagination parameters control page and size correctly

**Test description:**  
Call with page=0 size=5 and page=1 size=5; verify different items and correct pagination metadata.

**Expected result:**  
Both pages return 200; second page has different items; metadata correct.

**Actual result:**  
First page request was not OK (res.ok() false). Same environment/URL/token issue.

**Test result:** Failed

---

### Test 6: [PHN-2529] Empty result set returned cleanly when no customers match

**Test description:**  
Call with coordinates (0,0) that match no customers. Expect 200 with empty list and valid structure.

**Expected result:**  
HTTP 200, empty content array, valid pagination metadata.

**Actual result:**  
Response was not OK (res.ok() false). Same environment/URL/token issue.

**Test result:** Failed

---

### Test 7: [PHN-2529] Response includes recontract/acquisition classification field

**Test description:**  
Call endpoint and verify each customer item in response includes classification-related field (recontract/acquisition).

**Expected result:**  
Response includes classification field per customer; values present and consistent.

**Actual result:**  
Response was not OK (res.ok() false). Same environment/URL/token issue.

**Test result:** Failed

---

### Test 8: [PHN-2529] Classification values are consistent with backend logic

**Test description:**  
Verify classification values in response match backend logic for known test data.

**Expected result:**  
Classification values match backend rules.

**Actual result:**  
Response was not OK (res.ok() false). Same environment/URL/token issue.

**Test result:** Failed

---

## Notes

- **Tester:** Not set in Jira (customfield_10095 = null). Report sent only to #ai-report.
- **Environment:** Tests ran against BASE_URL from Playwright config (default or .env). The fix for PHN-2529 is on **experiment** branch; ensure the environment under test has the Sales Portal endpoint GET /sales-portal/customers/by-coordinates deployed and that the JWT used has access to that API.
- **Recommendation:** Re-run tests after deploying the experiment build to the target environment and/or configuring the correct Sales Portal base URL and valid token for that API.
