# PDT-2655 – Playwright test results

**Jira:** PDT-2655  
**Title:** [Backend] Invoice Cancelation does not work correctly, when user cancel invoice which is generated from SLP profile  
**Date:** 2026-03-17  
**Assignee:** Vato Chitaia  
**Tester:** Giorgi Kikriashvili  

**Spec (current):** `EnergoTS/tests/cursor/PDT-2655-invoice-cancellation-slp.spec.ts`  
**Base:** `EnergoTS/tests/billing/forVolumes/SLP.spec.ts` (For Volumes SLP flow – same fixtures and steps).

**Run command:** `npx playwright test tests/cursor/PDT-2655-invoice-cancellation-slp.spec.ts`  
**Branch:** cursor (Rule ENERGOTS.0).

---

## Spec summary

- **Total tests:** 24 (1 setup + 23 cancellation test cases).
- **Setup test:** Runs full SLP flow from For Volumes (customer → POD SLP → Meters → term → price parameter → price component „Измерено“ → product → contract → Activate POD → Data by profiles → Data by scales → Billing run → waitForInvoiceGeneration → get invoice numbers). Populates `slpInvoiceNumbers` for cancellation tests.
- **Cancellation tests (TC-1–TC-23):** Use `Endpoints.invoiceCancellation` and `Request.post(..., { data: { invoiceNumbers: [...] } })`; mapped from `test_cases/Flows/Invoice_cancellation_SLP/*.md`.

**Run status at report time:** Test run was started; Setup step waits for billing run (DRAFT) and invoice generation. Full run may take several minutes (Setup timeout 10 min). Re-run the command above for final passed/failed/skipped counts.

---

## Playwright test list (per test)

| # | Test title | What is verified |
|---|------------|------------------|
| 1 | [PDT-2655]: Setup – SLP flow to generate two invoices | SLP flow from For Volumes; capture invoice numbers for cancellation. |
| 2 | [PDT-2655] TC-1: Cancel both SLP-generated invoices in one request | POST with both numbers; expect success. |
| 3 | [PDT-2655] TC-2: Cancel first then second in two separate requests | Two sequential POSTs; both succeed. |
| 4 | [PDT-2655] TC-3: Cancellation with only one of the two SLP invoices | One invoice in request; success. |
| 5 | [PDT-2655] TC-4: Cancellation when one invoice is already cancelled | Second cancel on same invoice; expect 4xx. |
| 6 | [PDT-2655] TC-5: Non-existent or invalid invoice identifier | Invalid id; expect 400/404/422. |
| 7 | [PDT-2655] TC-6: ValidInvoice includes both SLP invoices | Both in payload; success. |
| 8 | [PDT-2655] TC-7: Request rejected when payload empty or missing identifiers | Empty body; expect 400. |
| 9 | [PDT-2655] TC-8: Request rejected when invoice identifier format invalid | Invalid format; expect 400/422. |
| 10 | [PDT-2655] TC-9: ValidInvoice excludes already-cancelled invoice | Cancel same again; expect 4xx. |
| 11 | [PDT-2655] TC-10: Revert applies to correct slot – first invoice only | Cancel first; success. |
| 12 | [PDT-2655] TC-11: Revert applies when cancelling second after first | Cancel first then second; both success. |
| 13 | [PDT-2655] TC-12: Second cancel fails when profile already reverted | Duplicate cancel; expect 4xx. |
| 14 | [PDT-2655] TC-13: Both invoices in one request – revert once per invoice | Single request with both; success. |
| 15 | [PDT-2655] TC-14: Idempotency – duplicate request returns error or no-op | Same two invoices twice; second 4xx. |
| 16 | [PDT-2655] TC-15: findInvoiceToCancel returns both SLP invoices | Both in request; success. |
| 17 | [PDT-2655] TC-16: Cancellation when billing run / SLP creation incomplete | Invalid invoice id; expect 4xx. |
| 18 | [PDT-2655] TC-17: Two invoice IDs where one is not SLP (mixed) | Mixed payload; success or 4xx. |
| 19 | [PDT-2655] TC-18: Order of cancellation (slot one first vs slot two first) | Both orders succeed. |
| 20 | [PDT-2655] TC-19: Double-revert prevented – same invoice cancelled twice | Second cancel 4xx. |
| 21 | [PDT-2655] TC-20: STANDARD profile cancellation (regression) | Placeholder id; status checked. |
| 22 | [PDT-2655] TC-21: ValidInvoice excludes invoice in invalid state | Reversed/closed id; expect 4xx. |
| 23 | [PDT-2655] TC-22: Mixed payload – one SLP and one non-SLP | Mixed; success or 4xx. |
| 24 | [PDT-2655] TC-23: Interim/debit-credit flows not broken by SLP cancellation | Single cancel; success. |

---

## How to run and get final results

1. From `EnergoTS/`:  
   `npx playwright test tests/cursor/PDT-2655-invoice-cancellation-slp.spec.ts --reporter=list`
2. Ensure `fixtures/token.json` and `fixtures/envVariables.json` exist (run `npx playwright test --project=setup` once if needed).
3. After the run completes, note: **Total: X passed, Y failed, Z skipped** and update this report if needed.

---

## Notes

- Tests that need two SLP invoices will skip if Setup did not populate `slpInvoiceNumbers` (e.g. invoice list API differs; adjust “Get generated invoice numbers” step to match backend).
- Framework: same as For Volumes SLP (`baseFixture`, `Request`, `GeneratePayload`, `Responses`, `Nomenclatures`, `Endpoints`).
