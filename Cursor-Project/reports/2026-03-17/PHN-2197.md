PHN-2197 – Playwright test results

Jira: PHN-2197
Title: Backend GET: POD list - Search of pods by Coordinates on Map view
Date: 2026-03-17
Assignee: "levani.iantbelidze" / Tester: "N/A" .

Total: 29 passed, 21 failed, 1 skipped.

-------------------------

Playwright test results:

Test 1: [PHN-2197] Request validation – TC-1: Existing /pod/list works unchanged when no coordinate params – baseline call without coordinate filters

Test description:
Call `GET /pod/list` with only basic paging parameters (no coordinate filters) and verify the endpoint responds successfully and returns a JSON payload.

Expected result:
Request succeeds (e.g. HTTP 200). Response body is valid JSON and compatible with existing callers.

Actual result:
Passed.

Test result:
Passed


Test 2: [PHN-2197] Request validation – TC-2: Coordinate params optional with existing filters – coordinate filtering does not break existing filters

Test description:
Call `GET /pod/list` (a) baseline; (b) with an example `bbox` parameter while keeping existing paging params.

Expected result:
Both requests succeed; coordinate params are optional and do not introduce new mandatory fields.

Actual result:
Passed.

Test result:
Passed


Test 3: [PHN-2197] Request validation – TC-3: Invalid coordinate format rejected (non-numeric) – invalid lat/lon/bbox rejected

Test description:
Call `GET /pod/list` with invalid coordinate values: non-numeric `lat`, non-numeric `lon`, and malformed `bbox`.

Expected result:
Client validation error (typically HTTP 400) for each invalid request; no server error.

Actual result:
Failed. At least one invalid-input request returned HTTP 206 (Partial Content) instead of the expected client validation error status.

Test result:
Failed


Test 4: [PHN-2197] Request validation – TC-4: Out-of-range lat/lon rejected – lat/lon ranges validated

Test description:
Call `GET /pod/list` with out-of-range latitude/longitude values (e.g. lat > 90, lon > 180).

Expected result:
Client validation error (typically HTTP 400) for each out-of-range request.

Actual result:
Failed. One or more out-of-range requests did not return the expected client validation error status.

Test result:
Failed


Test 5: [PHN-2197] Request validation – TC-5: Boundary lat/lon accepted (range edges) – range edges behave deterministically

Test description:
Call `GET /pod/list` with boundary values (lat = ±90, lon = ±180).

Expected result:
Request succeeds (typically HTTP 200) OR is rejected consistently if the product explicitly disallows boundaries; must not return 5xx.

Actual result:
Failed. At least one boundary request did not match the expected success criteria.

Test result:
Failed


Test 6: [PHN-2197] Request validation – TC-6: Partial coordinate input rejected (missing lat or lon) – reject incomplete coordinate pairs

Test description:
Call `GET /pod/list` with only `lat` (missing `lon`) and only `lon` (missing `lat`).

Expected result:
Client validation error (typically HTTP 400) for both requests (incomplete coordinate pair).

Actual result:
Failed. The `lat`-only request returned HTTP 206 (Partial Content) instead of the expected client validation error.

Test result:
Failed


Test 7: [PHN-2197] Request validation – TC-7: Empty result set returned cleanly when coordinate filter matches no PODs – empty content is valid

Test description:
Call `GET /pod/list` with a coordinate filter targeting an area expected to contain no PODs.

Expected result:
Request succeeds (e.g. HTTP 200) with an empty result set; response is well-formed and includes paging metadata if applicable.

Actual result:
Passed.

Test result:
Passed


Test 8: [PHN-2197] Request validation – TC-8: PODs without coordinates handled deterministically – null coordinate POD handling

Test description:
Call `GET /pod/list` with coordinate filtering and verify PODs without coordinates are handled consistently (excluded or included based on documented behaviour).

Expected result:
Deterministic behaviour; no server error; response schema remains valid.

Actual result:
Passed.

Test result:
Passed


Test 9: [PHN-2197] Request validation – TC-9: Conflicting coordinate filter modes (bbox + point/radius) rejected – conflict validation

Test description:
Call `GET /pod/list` with both `bbox` and `lat/lon` parameters in the same request.

Expected result:
Client validation error (typically HTTP 400) indicating conflicting filter modes are not allowed (unless explicitly supported).

Actual result:
Failed. The endpoint returned HTTP 206 (Partial Content) instead of the expected conflict validation error status.

Test result:
Failed


Test 10: [PHN-2197] Request validation – TC-10: Existing non-coordinate filters work with coordinate filtering – filter intersection

Test description:
Call `GET /pod/list` with coordinate filtering plus existing non-coordinate filters and verify results represent the intersection.

Expected result:
Request succeeds and returns results consistent with applying both filters together.

Actual result:
Failed. The request did not meet success criteria (`res.ok()` was false).

Test result:
Failed


Test 11: [PHN-2197] Request validation – TC-11: Invalid pagination params rejected with coordinate filter – paging validation

Test description:
Call `GET /pod/list` with invalid paging params (e.g. size=0, size=-1, page=-1, overly large size) while using coordinate filtering.

Expected result:
Invalid paging values are rejected (typically HTTP 400) or handled per documented constraints (e.g. capped max size); must not return 5xx.

Actual result:
Passed.

Test result:
Passed


Test 12: [PHN-2197] Request validation – TC-12: Sorting applied with coordinate filtering – sort correctness

Test description:
Call `GET /pod/list` with coordinate filtering and sorting parameters (`sortBy`, `sortDirection`) and verify sorting is applied.

Expected result:
Request succeeds and returns consistently sorted results.

Actual result:
Failed. Sorting request did not meet success criteria (`res.ok()` was false).

Test result:
Failed


Test 13: [PHN-2197] Request validation – TC-13: Unsupported sort field rejected (coordinate mode) – validation of sortBy

Test description:
Call `GET /pod/list` with an unsupported `sortBy` value alongside coordinate filtering.

Expected result:
Client validation error (typically HTTP 400) for unsupported sorting; must not return 5xx.

Actual result:
Passed.

Test result:
Passed


Test 14: [PHN-2197] Request validation – TC-14: Coordinate filtering via API gateway path – gateway route works

Test description:
Call `GET /pod/list` through the normal gateway route using coordinate parameters.

Expected result:
Request is routed successfully; response is returned (no gateway-level failure).

Actual result:
Passed.

Test result:
Passed


Test 15: [PHN-2197] Request validation – TC-15: Coordinate params with whitespace/locale decimal handled consistently – parsing consistency

Test description:
Call `GET /pod/list` with coordinate parameters containing whitespace and with a locale comma decimal variant to verify deterministic parsing/validation.

Expected result:
Requests are either accepted or rejected consistently (typically HTTP 200 or HTTP 400); must not return 5xx; no silent misinterpretation.

Actual result:
Failed. One or both parsing-variant requests did not meet the expected status constraints.

Test result:
Failed


Test 16: [PHN-2197] Bounding box – TC-1: Bbox search returns PODs inside box and excludes outside – bbox filter basics

Test description:
Call `GET /pod/list` with a valid `bbox` and verify the response is limited to PODs within the box (and excludes clearly outside areas).

Expected result:
Request succeeds and returns PODs within the bbox (as per system data).

Actual result:
Passed.

Test result:
Passed


Test 17: [PHN-2197] Bounding box – TC-2: PODs on bbox boundaries handled consistently – boundary inclusion/exclusion

Test description:
Call `GET /pod/list` with a bbox that includes known boundary points and verify consistent treatment.

Expected result:
Deterministic inclusion/exclusion for boundary PODs; no server error.

Actual result:
Passed.

Test result:
Passed


Test 18: [PHN-2197] Bounding box – TC-3: Bbox with min > max rejected – bbox validation

Test description:
Call `GET /pod/list` with bbox min greater than max (invalid).

Expected result:
Client validation error (typically HTTP 400).

Actual result:
Failed. Invalid bbox did not return the expected client validation error status.

Test result:
Failed


Test 19: [PHN-2197] Bounding box – TC-4: Bbox with missing or wrong number of coordinates rejected – bbox arity validation

Test description:
Call `GET /pod/list` with malformed bbox arity (2 values, 5 values, empty).

Expected result:
Client validation error (typically HTTP 400) for each malformed bbox.

Actual result:
Failed. One or more malformed bbox requests did not return the expected validation error status.

Test result:
Failed


Test 20: [PHN-2197] Bounding box – TC-5: Alternative bbox ordering accepted or rejected explicitly – deterministic interpretation

Test description:
Call `GET /pod/list` with an alternate bbox ordering (if applicable) and verify behaviour is explicit and consistent.

Expected result:
Either accepted with correct interpretation or rejected with a clear client error; no 5xx.

Actual result:
Passed.

Test result:
Passed


Test 21: [PHN-2197] Bounding box – TC-6: Bbox combined with existing filters returns intersection – filter intersection

Test description:
Call `GET /pod/list` with bbox and additional existing filters and verify results represent the intersection.

Expected result:
Request succeeds and results satisfy both bbox and the other filters.

Actual result:
Failed. The request did not meet success criteria (`res.ok()` was false).

Test result:
Failed


Test 22: [PHN-2197] Bounding box – TC-7: Zero-area bbox (min == max) behaves deterministically – degenerate bbox

Test description:
Call `GET /pod/list` with a zero-area bbox (min == max).

Expected result:
Deterministic behaviour (either empty or point match) and no server error.

Actual result:
Passed.

Test result:
Passed


Test 23: [PHN-2197] Bounding box – TC-8: Bbox values outside lat/lon ranges rejected – range validation

Test description:
Call `GET /pod/list` with bbox values outside valid lat/lon ranges.

Expected result:
Client validation error (typically HTTP 400) for each out-of-range request.

Actual result:
Failed. One or more out-of-range bbox requests did not return the expected validation error status.

Test result:
Failed


Test 24: [PHN-2197] Bounding box – TC-9: PODs without coordinates excluded from bbox results – null coordinate handling

Test description:
Call `GET /pod/list` with bbox filtering and verify PODs without coordinates are not returned (or are handled deterministically per spec).

Expected result:
Deterministic behaviour; no server error.

Actual result:
Passed.

Test result:
Passed


Test 25: [PHN-2197] Bounding box – TC-10: Dateline-crossing bbox supported or rejected clearly – dateline edge case

Test description:
Call `GET /pod/list` with a bbox that crosses the dateline (if supported) or verify it is rejected clearly.

Expected result:
Either supported with clear semantics or rejected with a client validation error; no 5xx.

Actual result:
Passed.

Test result:
Passed


Test 26: [PHN-2197] Bounding box – TC-11: Extremely large bbox respects max response constraints – safety/performance

Test description:
Call `GET /pod/list` with an extremely large bbox and verify behaviour is bounded (paging, limits, no 5xx).

Expected result:
No 5xx; response constrained by paging/limits; consistent behaviour.

Actual result:
Passed.

Test result:
Passed


Test 27: [PHN-2197] Bounding box – TC-12: Bbox and point/radius together rejected – conflict validation

Test description:
Call `GET /pod/list` with both bbox and point/radius params (conflict).

Expected result:
Client validation error (typically HTTP 400) unless explicitly supported.

Actual result:
Failed. The conflicting-filter request did not return the expected validation error status.

Test result:
Failed


Test 28: [PHN-2197] Point and radius – TC-1: Point + radius returns PODs within radius – proximity filter basics

Test description:
Call `GET /pod/list` with `lat`, `lon`, and `radius` and verify results are within radius (dataset dependent).

Expected result:
Request succeeds and returns PODs within radius; no 5xx.

Actual result:
Passed.

Test result:
Passed


Test 29: [PHN-2197] Point and radius – TC-2: Radius boundary inclusion consistent – boundary semantics

Test description:
Call proximity search with a boundary radius and verify consistent inclusion/exclusion.

Expected result:
Deterministic behaviour; no 5xx.

Actual result:
Passed.

Test result:
Passed


Test 30: [PHN-2197] Point and radius – TC-3: Non-numeric radius rejected – validation

Test description:
Call proximity search with non-numeric radius (and empty/missing radius variants).

Expected result:
Client validation error (typically HTTP 400) for invalid radius; no 5xx.

Actual result:
Failed. One or more invalid-radius requests did not return the expected validation error status.

Test result:
Failed


Test 31: [PHN-2197] Point and radius – TC-4: Negative radius rejected – validation

Test description:
Call proximity search with negative radius.

Expected result:
Client validation error (typically HTTP 400).

Actual result:
Failed. Negative radius did not return the expected validation error status.

Test result:
Failed


Test 32: [PHN-2197] Point and radius – TC-5: Radius = 0 behaves deterministically – degenerate radius

Test description:
Call proximity search with radius=0.

Expected result:
Deterministic behaviour (either rejected or treated as “point only”); no 5xx.

Actual result:
Passed.

Test result:
Passed


Test 33: [PHN-2197] Point and radius – TC-6: Point-only mode (if supported) returns nearest deterministically – optional point-only

Test description:
Call proximity search with lat/lon but without radius (if supported) and verify deterministic output or explicit validation error.

Expected result:
Either supported deterministically or rejected clearly; no 5xx.

Actual result:
Passed.

Test result:
Passed


Test 34: [PHN-2197] Point and radius – TC-7: Unreasonably large radius rejected or constrained – safety bounds

Test description:
Call proximity search with an unreasonably large radius.

Expected result:
Rejected (HTTP 400) or constrained explicitly; no 5xx.

Actual result:
Passed.

Test result:
Passed


Test 35: [PHN-2197] Point and radius – TC-8: Conflicting filters (radius + bbox) rejected – conflict validation

Test description:
Call `GET /pod/list` with both bbox and radius-based parameters (conflict).

Expected result:
Client validation error (typically HTTP 400) unless explicitly supported.

Actual result:
Failed. Conflict request did not return the expected validation error status.

Test result:
Failed


Test 36: [PHN-2197] Point and radius – TC-9: PODs without coordinates excluded from point/radius results – null coordinate handling

Test description:
Call proximity search and verify PODs without coordinates are not included (or are handled deterministically).

Expected result:
Deterministic behaviour; no 5xx.

Actual result:
Passed.

Test result:
Passed


Test 37: [PHN-2197] Point and radius – TC-10: Point/radius combined with existing filters returns intersection – filter intersection

Test description:
Call proximity search with additional existing filters and verify intersection behaviour.

Expected result:
Request succeeds; returned items match both coordinate and non-coordinate filters.

Actual result:
Failed. The request did not meet success criteria (`res.ok()` was false).

Test result:
Failed


Test 38: [PHN-2197] Point and radius – TC-11: Units (meters vs km) detected or bounded – units handling

Test description:
Call proximity search with two radius values representing different units and verify behaviour is bounded/deterministic.

Expected result:
No 5xx; behaviour is explicit and bounded.

Actual result:
Passed.

Test result:
Passed


Test 39: [PHN-2197] Point and radius – TC-12: Precision (many decimal places) works without rounding errors – precision handling

Test description:
Call proximity search with high-precision lat/lon values.

Expected result:
Request succeeds or rejects consistently; no 5xx.

Actual result:
Passed.

Test result:
Passed


Test 40: [PHN-2197] Pagination/sort/regression – TC-1: Pagination works with bbox (disjoint pages) – paging with bbox

Test description:
Call bbox search with `page`/`size` and verify paging is consistent.

Expected result:
Requests succeed; pages are returned consistently.

Actual result:
Passed.

Test result:
Passed


Test 41: [PHN-2197] Pagination/sort/regression – TC-2: Page beyond last page handled gracefully – paging edge case

Test description:
Call bbox search with a page index beyond the last page.

Expected result:
Request succeeds and returns empty content or a defined behaviour; no 5xx.

Actual result:
Passed.

Test result:
Passed


Test 42: [PHN-2197] Pagination/sort/regression – TC-3: Sorting with bbox (ASC vs DESC consistent) – sort direction behaviour

Test description:
Call bbox search with `sortBy` and `sortDirection=ASC` and `DESC` and verify both succeed.

Expected result:
Both requests succeed and return deterministically sorted results.

Actual result:
Failed. One or both sorted requests were not successful (`ok()` false).

Test result:
Failed


Test 43: [PHN-2197] Pagination/sort/regression – TC-4: Sort + pagination no duplicates for stable sort – stability across pages

Test description:
Fetch multiple pages with stable sorting and check that all page requests succeed and there are no duplicates across pages.

Expected result:
All page requests succeed; stable ordering; no duplicates.

Actual result:
Failed. At least one page request was not successful (`ok()` false), so stability check could not pass.

Test result:
Failed


Test 44: [PHN-2197] Pagination/sort/regression – TC-5: Coordinate search does not alter response schema – schema compatibility

Test description:
Call the endpoint with coordinate filters and verify the response schema stays compatible (e.g. paged `content` or equivalent expected fields).

Expected result:
Request succeeds and response schema remains compatible for consumers.

Actual result:
Passed.

Test result:
Passed


Test 45: [PHN-2197] Pagination/sort/regression – TC-6: PODs without coordinates do not break pagination/sort – null coordinate robustness

Test description:
Call bbox search with sorting while the dataset includes PODs without coordinates.

Expected result:
Request succeeds; pagination/sort not broken by null coordinate PODs.

Actual result:
Failed. The request was not successful (`ok()` false).

Test result:
Failed


Test 46: [PHN-2197] Pagination/sort/regression – TC-7: Panning/zooming – small bbox changes give incremental results – map interaction behaviour

Test description:
Call bbox search with two slightly different bboxes (simulating map pan/zoom) and verify deterministic, incremental changes.

Expected result:
Requests succeed; result sets change deterministically.

Actual result:
Passed.

Test result:
Passed


Test 47: [PHN-2197] Pagination/sort/regression – TC-8: Contract-related flows that list/select POD still work – UI flow dependency

Test description:
Contract-related flows that select POD (typically UI + backend interactions) remain functional when coordinate params are introduced.

Expected result:
Flow remains functional; no regressions for contract-related POD selection.

Actual result:
Skipped. Requires UI/contract flow automation outside API-only scope.

Test result:
Not run


Test 48: [PHN-2197] Pagination/sort/regression – TC-9: Unknown parameters handled consistently – robustness

Test description:
Call `GET /pod/list` with unknown query parameters alongside known ones and verify deterministic handling.

Expected result:
Request succeeds or returns a clear client error; must not return 5xx.

Actual result:
Passed.

Test result:
Passed


Test 49: [PHN-2197] Pagination/sort/regression – TC-10: API gateway forwards coordinate params correctly – gateway integration

Test description:
Call bbox search with sorting through the gateway route to verify coordinate params are forwarded correctly.

Expected result:
Request succeeds through the gateway and returns results.

Actual result:
Failed. The request was not successful (`ok()` false).

Test result:
Failed


Test 50: [PHN-2197] Pagination/sort/regression – TC-11: Rapid successive map requests do not cause 5xx – resilience under repeated queries

Test description:
Send a short burst of coordinate-filtered requests (simulating rapid map interactions) and verify no 5xx responses occur.

Expected result:
No 5xx responses; system remains stable.

Actual result:
Passed.

Test result:
Passed


Test 51: [PHN-2197] Pagination/sort/regression – TC-12: Existing /pod/list tests remain valid (backward compatible) – regression

Test description:
Re-run a baseline request pattern and verify compatibility with existing `/pod/list` consumers after coordinate parameters are introduced.

Expected result:
Baseline requests still succeed; no breaking changes.

Actual result:
Passed.

Test result:
Passed

