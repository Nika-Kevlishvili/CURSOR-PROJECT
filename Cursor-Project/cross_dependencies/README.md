# cross_dependencies/

JSON outputs of the cross-dependency-finder workflow, one file per analyzed scope.

Naming: `YYYY-MM-DD_<TICKET-KEY-or-topic>.json`

This folder ships empty in the toolkit. Files are produced on demand by the
cross-dependency-finder skill/agent (see `.cursor/skills/cross-dependency-finder/`).
