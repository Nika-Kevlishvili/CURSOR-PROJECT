---
alwaysApply: true
---

# CRITICAL REMINDERS

1. **Rule 0.0** - read all `.cursor/rules/**.mdc` before acting. **CONF.0** - if scope/env/target/success is ambiguous, ASK before substantive action; do not guess.
2. **Rule 0.1** - end every response with `Agents involved: [names]`.
3. **Rule 0.2** - ALL Phoenix questions go to PhoenixExpert.
4. **Rule 0.6** - reports per `Cursor-Project/reports/README.md` (chat by default; file on `/report` or workflow mandate).
5. **Rule 0.8** - Phoenix is NEVER edited by AI; EnergoTS only via EnergoTSTestAgent under `tests/`; other workspace paths editable on user request.
6. **Rule 1** - GitLab and Confluence are READ-ONLY.
7. **Rule 8** - consult PhoenixExpert before tasks.
8. **Rule 32** - bug validation -> `bug-validator` subagent.
9. **Rule 35 / 35a** - test cases: cross-dependency-finder FIRST (Jira + codebase + shallow Confluence; no local merge/git), then test-case-generator (two files: Backend + Frontend).
10. **Rule 36 + ENERGOTS.0** - Playwright runs from EnergoTS only on the `cursor` branch via `energo-ts-run`.
11. **Rule 37** - Jira + `/HandsOff` -> full HandsOff orchestrator; report named after Jira key; Slack to Tester + #ai-report.
12. **Rule JIRA.0** - Jira bugs go on the **Experiments** board only; never Phoenix delivery.
13. **Rule PDR.0** - production data -> `production-data-reader` (PostgreSQLProd MCP, readonly).
14. **Rule CONF.1** - every deliverable ends with `**Confidence: XX%**` + reason.

Rule files live recursively under `.cursor/rules/main|safety|agents|workflows|workspace|integrations/`. Nested Phoenix repos may define their own `.cursor/rules/` - those apply to their paths and are not duplicates.
